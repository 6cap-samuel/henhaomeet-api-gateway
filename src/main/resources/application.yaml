spring:
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: ${CORS}
      routes:
        - id: ${MEETING_ROUTE_ID}
          uri: ${MEETING_ROUTE_URI}
          predicates:
            - Path=${MEETING_ROUTE_PREDICATE}

